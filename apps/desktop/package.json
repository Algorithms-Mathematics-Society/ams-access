{
  "name": "@ams/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mac-dev": "APPLE_SIGNING_IDENTITY='AMS Access Dev' tauri build --debug --bundles app",
    "setup:mac-signing": "bash src-tauri/scripts/setup-macos-dev-signing.sh",
    "clean": "rm -rf src-tauri/target"
  },
  "dependencies": {
    "@ams/web": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
